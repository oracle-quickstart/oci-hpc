{% raw %}
# This file is generated by scripts/generate_mgmt_bash_completion.py.
# Do not edit it by hand.
#
# Example regeneration command from a deployed controller:
#   scripts/generate_mgmt_bash_completion.py --mgmt-cmd "mgmt" --output playbooks/roles/mgmt/templates/mgmt.bash_completion.j2

declare -A _MGMT_SUBCOMMANDS=(

    ["__root__"]="clusters configurations database fabrics images login network nodes recommendations services status"
    ["clusters"]="add create delete list update-instance-config"
    ["configurations"]="create delete get list update update-slurm"
    ["database"]="add create delete export scan-vcn update"
    ["fabrics"]="list"
    ["images"]="add-shape create list"
    ["login"]="create delete list"
    ["network"]="blocks rails"
    ["nodes"]="boot-volume-swap console-history get healthchecks list reboot reconfigure tag tag-and-terminate terminate"
    ["recommendations"]="list run"
    ["services"]="active-hc all ansible init multi-node-hc scan-host-api scan-queue update-metadata"
    ["status"]=""
    ["clusters add"]="memory-fabric node"
    ["clusters create"]=""
    ["clusters delete"]=""
    ["clusters list"]=""
    ["clusters update-instance-config"]=""
    ["configurations create"]="from-existing from-file"
    ["configurations delete"]=""
    ["configurations get"]=""
    ["configurations list"]=""
    ["configurations update"]=""
    ["configurations update-slurm"]=""
    ["database add"]=""
    ["database create"]=""
    ["database delete"]=""
    ["database export"]=""
    ["database scan-vcn"]=""
    ["database update"]=""
    ["fabrics list"]=""
    ["images add-shape"]=""
    ["images create"]=""
    ["images list"]=""
    ["login create"]=""
    ["login delete"]=""
    ["login list"]=""
    ["network blocks"]="list"
    ["network rails"]="list"
    ["nodes boot-volume-swap"]=""
    ["nodes console-history"]=""
    ["nodes get"]="any ids ips names serials"
    ["nodes healthchecks"]=""
    ["nodes list"]=""
    ["nodes reboot"]=""
    ["nodes reconfigure"]=""
    ["nodes tag"]=""
    ["nodes tag-and-terminate"]=""
    ["nodes terminate"]=""
    ["recommendations list"]=""
    ["recommendations run"]=""
    ["services active-hc"]=""
    ["services all"]=""
    ["services ansible"]=""
    ["services init"]=""
    ["services multi-node-hc"]=""
    ["services scan-host-api"]=""
    ["services scan-queue"]=""
    ["services update-metadata"]=""
    ["clusters add memory-fabric"]=""
    ["clusters add node"]=""
    ["configurations create from-existing"]=""
    ["configurations create from-file"]=""
    ["network blocks list"]="cluster"
    ["network rails list"]="cluster"
    ["nodes get any"]=""
    ["nodes get ids"]=""
    ["nodes get ips"]=""
    ["nodes get names"]=""
    ["nodes get serials"]=""
    ["network blocks list cluster"]=""
    ["network rails list cluster"]=""
)

declare -A _MGMT_OPTIONS=(
    ["__root__"]="--config --debug --no-debug --clush-parallel-executions --active-healthchecks --no-active-healthchecks --active-healthchecks-frequency --multi-nodes-healthchecks --no-multi-nodes-healthchecks --multi-nodes-healthchecks-frequency --help"
    ["clusters"]="--help"
    ["configurations"]="--help"
    ["database"]="--help"
    ["fabrics"]="--help"
    ["images"]="--help"
    ["login"]="--help"
    ["network"]="--help"
    ["nodes"]="--help"
    ["recommendations"]="--help"
    ["services"]="--help"
    ["status"]="--no_color --help"
    ["clusters add"]="--help"
    ["clusters create"]="--count --cluster --instancetype --names --fabric --memorycluster --help"
    ["clusters delete"]="--cluster --memory_cluster --help"
    ["clusters list"]="--format --help"
    ["clusters update-instance-config"]="--cluster-name --image-id --ssh-key --cloud-init --boot-volume-size --display-name --bvr --bvr-size --help"
    ["configurations create"]="--help"
    ["configurations delete"]="--configuration --help"
    ["configurations get"]="--name --help"
    ["configurations list"]="--format --output_file --partition --role --shape --help"
    ["configurations update"]="--name --fields --help"
    ["configurations update-slurm"]="--slurm-conf --dry-run --help"
    ["database add"]="--ip --hostname --ocid --help"
    ["database create"]="--help"
    ["database delete"]="--nodes --fields --help"
    ["database export"]="--filename --use-base --help"
    ["database scan-vcn"]="--dns --change_hostname --help"
    ["database update"]="--fields --help"
    ["fabrics list"]="--full --help"
    ["images add-shape"]="--image --compartment --shape --help"
    ["images create"]="--url --compartment --help"
    ["images list"]="--format --used --compartment --help"
    ["login create"]="--count --configuration --names --help"
    ["login delete"]="--hostname --help"
    ["login list"]="--format --help"
    ["network blocks"]="--help"
    ["network rails"]="--help"
    ["nodes boot-volume-swap"]="--nodes --fields --image --size --help"
    ["nodes console-history"]="--nodes --fields --help"
    ["nodes get"]="--help"
    ["nodes healthchecks"]="--nodes --fields --type --exclude-node --reservation --help"
    ["nodes list"]="--one-line --cluster --memory-cluster --style --format --width --columns --no-header --fields --terminated --terminated_delay --help"
    ["nodes reboot"]="--nodes --fields --soft --help"
    ["nodes reconfigure"]="--nodes --fields --action --command --playbook --help"
    ["nodes tag"]="--nodes --fields --help"
    ["nodes tag-and-terminate"]="--nodes --fields --help"
    ["nodes terminate"]="--nodes --fields --help"
    ["recommendations list"]="--healthcheck --unreachable --unconfigured --unreachable_timeout --unconfigured_timeout --help"
    ["recommendations run"]="--nodes --healthcheck --unreachable --unconfigured --unreachable_timeout --unconfigured_timeout --help"
    ["services active-hc"]="--help"
    ["services all"]="--http_port --help"
    ["services ansible"]="--help"
    ["services init"]="--help"
    ["services multi-node-hc"]="--help"
    ["services scan-host-api"]="--help"
    ["services scan-queue"]="--help"
    ["services update-metadata"]="--nodes --http_port --help"
    ["clusters add memory-fabric"]="--count --cluster --fabric --memorycluster --instancetype --help"
    ["clusters add node"]="--count --cluster --names --memorycluster --help"
    ["configurations create from-existing"]="--configuration --name --help"
    ["configurations create from-file"]="--file --help"
    ["network blocks list"]="--help"
    ["network rails list"]="--help"
    ["nodes get any"]="--format --help"
    ["nodes get ids"]="--format --help"
    ["nodes get ips"]="--format --help"
    ["nodes get names"]="--format --help"
    ["nodes get serials"]="--format --help"
    ["network blocks list cluster"]="--cluster --help"
    ["network rails list cluster"]="--cluster --nodes --no-nodes --help"
)

declare -A _MGMT_VALUE_OPTIONS=(
    ["__root__"]="--config --clush-parallel-executions --active-healthchecks-frequency --multi-nodes-healthchecks-frequency"
    ["clusters"]=""
    ["configurations"]=""
    ["database"]=""
    ["fabrics"]=""
    ["images"]=""
    ["login"]=""
    ["network"]=""
    ["nodes"]=""
    ["recommendations"]=""
    ["services"]=""
    ["status"]=""
    ["clusters add"]=""
    ["clusters create"]="--count --cluster --instancetype --names --fabric --memorycluster"
    ["clusters delete"]="--cluster --memory_cluster"
    ["clusters list"]="--format"
    ["clusters update-instance-config"]="--cluster-name --image-id --ssh-key --cloud-init --boot-volume-size --display-name --bvr-size"
    ["configurations create"]=""
    ["configurations delete"]="--configuration"
    ["configurations get"]="--name"
    ["configurations list"]="--format --output_file --partition --role --shape"
    ["configurations update"]="--name --fields"
    ["configurations update-slurm"]="--slurm-conf"
    ["database add"]="--ip --hostname --ocid"
    ["database create"]=""
    ["database delete"]="--nodes --fields"
    ["database export"]="--filename"
    ["database scan-vcn"]=""
    ["database update"]="--fields"
    ["fabrics list"]=""
    ["images add-shape"]="--image --compartment --shape"
    ["images create"]="--url --compartment"
    ["images list"]="--format --compartment"
    ["login create"]="--count --configuration --names"
    ["login delete"]="--hostname"
    ["login list"]="--format"
    ["network blocks"]=""
    ["network rails"]=""
    ["nodes boot-volume-swap"]="--nodes --fields --image --size"
    ["nodes console-history"]="--nodes --fields"
    ["nodes get"]=""
    ["nodes healthchecks"]="--nodes --fields --type --exclude-node --reservation"
    ["nodes list"]="--cluster --memory-cluster --style --format --width --columns --fields --terminated_delay"
    ["nodes reboot"]="--nodes --fields"
    ["nodes reconfigure"]="--nodes --fields --action --command --playbook"
    ["nodes tag"]="--nodes --fields"
    ["nodes tag-and-terminate"]="--nodes --fields"
    ["nodes terminate"]="--nodes --fields"
    ["recommendations list"]="--unreachable_timeout --unconfigured_timeout"
    ["recommendations run"]="--nodes --unreachable_timeout --unconfigured_timeout"
    ["services active-hc"]=""
    ["services all"]="--http_port"
    ["services ansible"]=""
    ["services init"]=""
    ["services multi-node-hc"]=""
    ["services scan-host-api"]=""
    ["services scan-queue"]=""
    ["services update-metadata"]="--nodes --http_port"
    ["clusters add memory-fabric"]="--count --cluster --fabric --memorycluster --instancetype"
    ["clusters add node"]="--count --cluster --names --memorycluster"
    ["configurations create from-existing"]="--configuration --name"
    ["configurations create from-file"]="--file"
    ["network blocks list"]=""
    ["network rails list"]=""
    ["nodes get any"]="--format"
    ["nodes get ids"]="--format"
    ["nodes get ips"]="--format"
    ["nodes get names"]="--format"
    ["nodes get serials"]="--format"
    ["network blocks list cluster"]="--cluster"
    ["network rails list cluster"]="--cluster"
)


_mgmt_click_completion() {
    local IFS=$'\n'
    local response
    local completion
    local type
    local value
    local -a click_words
    local click_cword

    click_words=("${_MGMT_WORDS[@]}")
    click_cword="${_MGMT_CWORD}"
    if [[ -z "${_MGMT_RAW_CURRENT}" ]] && [[ "${COMP_LINE}" != *" " ]] && [[ "${_MGMT_CURRENT}" == *"=" || "${_MGMT_CURRENT}" == *,* ]]; then
        click_words+=("")
        click_cword=$((${#click_words[@]} - 1))
    fi

    response=$(
        env \
            COMP_WORDS="${click_words[*]}" \
            COMP_CWORD="${click_cword}" \
            COMP_POINT="${COMP_POINT:-0}" \
            COMP_TYPE="${COMP_TYPE:-0}" \
            _MGMT_COMPLETE=bash_complete \
            "$1"
    )

    COMPREPLY=()
    for completion in $response; do
        IFS=',' read -r type value <<< "${completion}"
        case "${type}" in
            dir)
                compopt -o dirnames
                ;;
            file)
                compopt -o default
                ;;
        esac
        COMPREPLY+=("${value}")
    done

    return 0
}

_mgmt_compgen_words() {
    local current="$1"
    shift
    COMPREPLY=( $(compgen -W "$*" -- "${current}") )
}

_mgmt_filter_used_options() {
    local filtered=()
    local candidate
    local used

    for candidate in "${COMPREPLY[@]}"; do
        used=0
        if [[ "${candidate}" == --* ]]; then
            for word in "${_MGMT_WORDS[@]}"; do
                if [[ "${word}" == "${candidate}" ]]; then
                    used=1
                    break
                fi
            done
        fi
        if (( used == 0 )); then
            filtered+=("${candidate}")
        fi
    done

    COMPREPLY=("${filtered[@]}")
}

_mgmt_configure_compopt() {
    local candidate

    compopt +o nospace >/dev/null 2>&1 || true
    for candidate in "${COMPREPLY[@]}"; do
        if [[ "${candidate}" == *= ]]; then
            compopt -o nospace >/dev/null 2>&1 || true
            return
        fi
    done
}

_mgmt_key_for_path() {
    if (( $# == 0 )); then
        printf '%s' "__root__"
    else
        printf '%s' "$*"
    fi
}

_mgmt_option_requires_value() {
    local key="$1"
    local option="$2"
    [[ " ${_MGMT_VALUE_OPTIONS[$key]} " == *" ${option} "* ]]
}

_mgmt_prepare_words() {
    if declare -F _init_completion >/dev/null 2>&1; then
        local cur prev words cword
        _init_completion -n =,: || return
        _MGMT_RAW_CURRENT="${cur}"
        _MGMT_CURRENT="${cur}"
        _MGMT_PREV="${prev}"
        _MGMT_WORDS=("${words[@]}")
        _MGMT_CWORD="${cword}"
    elif declare -F _get_comp_words_by_ref >/dev/null 2>&1; then
        local cur prev words cword
        _get_comp_words_by_ref -n =,: cur prev words cword
        _MGMT_RAW_CURRENT="${cur}"
        _MGMT_CURRENT="${cur}"
        _MGMT_PREV="${prev}"
        _MGMT_WORDS=("${words[@]}")
        _MGMT_CWORD="${cword}"
    else
        _MGMT_RAW_CURRENT="${COMP_WORDS[COMP_CWORD]}"
        _MGMT_CURRENT="${COMP_WORDS[COMP_CWORD]}"
        _MGMT_PREV="${COMP_WORDS[COMP_CWORD-1]}"
        _MGMT_WORDS=("${COMP_WORDS[@]}")
        _MGMT_CWORD="${COMP_CWORD}"
    fi

    if [[ "${COMP_LINE}" == *" " ]] && (( ${#_MGMT_WORDS[@]} > 0 )) && (( _MGMT_CWORD == ${#_MGMT_WORDS[@]} - 1 )) && [[ -n "${_MGMT_CURRENT}" ]]; then
        _MGMT_WORDS+=("")
        _MGMT_CWORD=$((${#_MGMT_WORDS[@]} - 1))
        _MGMT_PREV="${_MGMT_CURRENT}"
        _MGMT_CURRENT=""
    fi
}

_mgmt_adjust_reply_for_raw_current() {
    local logical_current="$1"
    local raw_current="$2"
    local prefix
    local adjusted=()
    local candidate

    if [[ "${COMP_LINE}" == *" " ]]; then
        return
    fi

    if [[ "${logical_current}" == *"="* || "${logical_current}" == *","* ]]; then
        prefix="${logical_current%${logical_current##*[=,]}}"
    elif [[ "${logical_current}" != "${raw_current}" && -n "${raw_current}" ]]; then
        prefix="${logical_current%"${raw_current}"}"
    elif [[ "${logical_current}" != "${raw_current}" ]]; then
        prefix="${logical_current}"
    else
        return
    fi

    for candidate in "${COMPREPLY[@]}"; do
        if [[ -n "${prefix}" && "${candidate}" == "${prefix}"* ]]; then
            adjusted+=("${candidate#${prefix}}")
        else
            adjusted+=("${candidate}")
        fi
    done

    COMPREPLY=("${adjusted[@]}")
}

_mgmt_normalize_split_words() {
    local -a normalized=()
    local idx=0
    local new_index=-1
    local word

    for (( idx=0; idx<${#_MGMT_WORDS[@]}; idx++ )); do
        word="${_MGMT_WORDS[idx]}"

        if [[ "${word}" == "=" || "${word}" == "," ]]; then
            if (( ${#normalized[@]} == 0 )); then
                normalized+=("${word}")
                new_index=0
            else
                normalized[new_index]+="${word}"
            fi
            if (( idx == _MGMT_CWORD )); then
                _MGMT_CWORD="${new_index}"
            fi
            continue
        fi

        if (( ${#normalized[@]} > 0 )) && [[ "${normalized[new_index]}" == *= || "${normalized[new_index]}" == *, ]]; then
            normalized[new_index]+="${word}"
            if (( idx == _MGMT_CWORD )); then
                _MGMT_CWORD="${new_index}"
            fi
            continue
        fi

        normalized+=("${word}")
        ((new_index++))
        if (( idx == _MGMT_CWORD )); then
            _MGMT_CWORD="${new_index}"
        fi
    done

    _MGMT_WORDS=("${normalized[@]}")
    _MGMT_CURRENT="${_MGMT_WORDS[_MGMT_CWORD]}"
    if (( _MGMT_CWORD > 0 )); then
        _MGMT_PREV="${_MGMT_WORDS[_MGMT_CWORD-1]}"
    else
        _MGMT_PREV=""
    fi
}

_mgmt_resolve_path() {
    local key="__root__"
    local path_words=()
    local word
    local candidate_key
    local skip_next=0

    for (( i=1; i<_MGMT_CWORD; i++ )); do
        word="${_MGMT_WORDS[i]}"
        [[ -z "${word}" ]] && continue

        if (( skip_next )); then
            skip_next=0
            continue
        fi

        if [[ "${word}" == --* ]]; then
            if _mgmt_option_requires_value "${key}" "${word}" || _mgmt_option_requires_value "__root__" "${word}"; then
                skip_next=1
            fi
            continue
        fi

        candidate_key="$(_mgmt_key_for_path "${path_words[@]}" "${word}")"
        if [[ " ${_MGMT_SUBCOMMANDS[$key]} " == *" ${word} "* ]]; then
            path_words+=("${word}")
            key="${candidate_key}"
        fi
    done

    printf '%s' "${key}"
}

_mgmt_completion() {
    COMPREPLY=()
    local current prev key subcommands options logical_current
    _mgmt_prepare_words
    _mgmt_normalize_split_words
    current="${_MGMT_CURRENT}"
    prev="${_MGMT_PREV}"
    logical_current="${current}"
    if [[ "${COMP_LINE}" == *" " ]] && [[ -n "${current}" ]]; then
        prev="${current}"
        current=""
    fi
    key="$(_mgmt_resolve_path)"
    subcommands="${_MGMT_SUBCOMMANDS[$key]}"
    options="${_MGMT_OPTIONS[$key]}"

    if _mgmt_option_requires_value "${key}" "${prev}" || _mgmt_option_requires_value "__root__" "${prev}"; then
        _mgmt_click_completion "$1"
        if (( ${#COMPREPLY[@]} > 0 )); then
            _mgmt_adjust_reply_for_raw_current "${logical_current}" "${_MGMT_RAW_CURRENT}"
            _mgmt_configure_compopt
            return 0
        fi
    fi

    if [[ "${current}" == -* ]]; then
        _mgmt_compgen_words "${current}" ${options}
        _mgmt_filter_used_options
        _mgmt_configure_compopt
        return 0
    fi

    if [[ -n "${subcommands}" ]]; then
        _mgmt_compgen_words "${current}" ${subcommands}
        if (( ${#COMPREPLY[@]} > 0 )); then
            _mgmt_configure_compopt
            return 0
        fi
    fi

    if [[ -z "${current}" && -n "${options}" ]]; then
        _mgmt_compgen_words "${current}" ${options}
        _mgmt_filter_used_options
        _mgmt_configure_compopt
        return 0
    fi

    _mgmt_click_completion "$1"
    if (( ${#COMPREPLY[@]} > 0 )); then
        _mgmt_adjust_reply_for_raw_current "${logical_current}" "${_MGMT_RAW_CURRENT}"
        _mgmt_filter_used_options
        _mgmt_configure_compopt
        return 0
    fi
}

complete -o nosort -F _mgmt_completion mgmt
{% endraw %}
