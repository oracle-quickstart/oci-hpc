[Unit]
Description=NCCL Profiler Trace Exporter
After=network.target

[Service]
User={{ ansible_user }}
Group={{ ansible_user }}
Type=simple
{% if (groups['monitoring']| length ) > 0 %}
ExecStart=/usr/bin/python3 {{ nccl_exporter_script }} --tempo-endpoint  {{ hostvars[groups['monitoring'][0]]['monitoring_hostname']}}:4317
{% else %}
ExecStart=/usr/bin/python3 {{ nccl_exporter_script }} --tempo-endpoint  {{ hostvars[groups['controller'][0]]['controller_hostname']}}:4317
{% endif %}

[Install]
WantedBy=multi-user.target
