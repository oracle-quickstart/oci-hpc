global:
  scrape_interval: 60s
  external_labels:
    monitor: 'prometheus'

rule_files:
- recording_rules.yml

scrape_configs:
- job_name: prometheus
  file_sd_configs:
  - files:
    - '/etc/prometheus/targets/*.json'