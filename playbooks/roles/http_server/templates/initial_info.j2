{
    "AD": "{{ host_info.availabilityDomain }}",
    "cluster_name": "{{ host_info.cluster_name }}",
    "compartment": "{{ host_info.compartmentId }}",
    "controller_name": "{{ host_info.controller_name }}",
    "fss_mount": "{{ host_info.fss_ip }}",
    "hpc_island": "{{ host_info.hpc_island }}",
    "hostname": "{{ host_info.host }}",
    "networkBlockId": "{{ host_info.networkBlockId }}",
    "oci_name": "{{ host_info.oci_name }}",
    "ocid": "{{ host_info.ocid }}",
    "rackID": "{{ host_info.rackID }}",
    "railId": "{{ host_info.railId }}",
    "role": "{{ host_info.role }}",
    "serial": "{{ host_info.serial_number }}",
    "shape": "{{ host_info.shape }}",
    "status": "{{ http_server_status }}"
}