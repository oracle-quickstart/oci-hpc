---
- block:

  - name: Make sure python OpenSSL and parallel ssh is installed
    apt:
      name:
        - python-openssl
        - python-cryptography
        - parted
        - pssh
        - pdsh
        - python3-netaddr

