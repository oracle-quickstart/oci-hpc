---
- name: Enable ol7_developer_EPEL repo
  ansible.builtin.command: yum-config-manager --enable ol7_developer_EPEL
- name: Make sure python OpenSSL and parallel ssh is installed
  vars:
    package_name:
      - pyOpenSSL
      - python2-cryptography
      - python3-oci-cli
      - pssh
      - pdsh
      - python3-pip
    package_state: latest
    package_repo: "ol7_developer_EPEL"
  ansible.builtin.include_role:
    name: safe_yum

- name: Upgrade Pip3
  become: true
  ansible.builtin.pip:
    name: [pip]
    state: latest
    executable: /usr/bin/pip3
  ignore_errors: true
