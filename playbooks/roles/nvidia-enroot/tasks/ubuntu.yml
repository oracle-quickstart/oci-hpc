---
- block:
    - name: Get DEB architecture # noqa no-changed-when
      ansible.builtin.command: dpkg --print-architecture
      register: deb_architecture

    - name: Set DPKG architecture
      ansible.builtin.set_fact:
        dpkg_arch: "{{ deb_architecture.stdout }}"

    - name: Ensure nvidia-enroot package cache directory exists
      ansible.builtin.file:
        path: "{{ nvidia_enroot_download_path }}"
        state: directory
        mode: "0755"

    - name: Check if required debs are already downloaded
      ansible.builtin.stat:
        path: "{{ nvidia_enroot_download_path }}/{{ item | basename }}"
      register: deb_files
      with_items:
        - "{{ enroot_file }}"
        - "{{ enroot_caps_file }}"

    - name: Download DEBs if not already present
      ansible.builtin.get_url:
        url: "{{ item.item }}"
        dest: "{{ nvidia_enroot_download_path }}/{{ item.item | basename }}"
        mode: "0755"
      when: not item.stat.exists
      with_items: "{{ deb_files.results }}"

    - name: Install required packages
      vars:
        deb_name:
          - "{{ nvidia_enroot_download_path }}/{{ enroot_file | basename }}"
          - "{{ nvidia_enroot_download_path }}/{{ enroot_caps_file | basename }}"
        package_state: present
      ansible.builtin.include_role:
        name: safe_yum

    - name: Set kernel.unprivileged_userns_clone using sysctl
      become: true
      ansible.posix.sysctl:
        name: kernel.unprivileged_userns_clone
        value: "1"
        sysctl_file: /etc/sysctl.d/81-enroot.conf
        reload: true

    - name: Check if enroot check DEBs are already downloaded
      ansible.builtin.stat:
        path: "{{ nvidia_enroot_download_path }}/{{ enroot_check_file | basename }}"
      register: enroot_check_files

    - name: Download enroot-check
      ansible.builtin.get_url:
        url: "{{ enroot_check_file }}"
        dest: "{{ nvidia_enroot_download_path }}"
        mode: "0755"
      when: not enroot_check_files.stat.exists | bool

    - name: Copy enroot-check to local /tmp
      ansible.builtin.copy:
        src: "{{ nvidia_enroot_download_path }}/{{ enroot_check_file | basename }}"
        dest: "/tmp"
        mode: "0755"

    - name: Execute enroot-check_*.run
      ansible.builtin.command: bash -c "/tmp/enroot-check_*.run --verify"
    - name: Set enroot_top_path
      ansible.builtin.set_fact:
        enroot_top_path_checked: "/etc/enroot/"
      when: " not 'nvme0n1' in ansible_facts[\"devices\"]"

    - name: Set enroot_top_path
      ansible.builtin.set_fact:
        enroot_top_path_checked: "{{ enroot_top_path }}"
      when: "'nvme0n1' in ansible_facts[\"devices\"]"

    - name: Update ENROOT_RUNTIME_PATH
      become: true
      ansible.builtin.lineinfile:
        dest: /etc/enroot/enroot.conf
        state: present
        regexp: "^#ENROOT_RUNTIME_PATH.*"
        line: "ENROOT_RUNTIME_PATH        {{ enroot_top_path_checked }}/enroot_runtime/user-$(id -u)"
        backup: true

    - name: Update ENROOT_DATA_PATH
      become: true
      ansible.builtin.lineinfile:
        dest: /etc/enroot/enroot.conf
        state: present
        regexp: "^#ENROOT_DATA_PATH.*"
        line: "ENROOT_DATA_PATH        {{ enroot_top_path_checked }}/enroot_data/user-$(id -u)"
        backup: true

    - name: Update ENROOT_CACHE_PATH
      become: true
      ansible.builtin.lineinfile:
        dest: /etc/enroot/enroot.conf
        state: present
        regexp: "^#ENROOT_CACHE_PATH.*"
        line: "ENROOT_CACHE_PATH        {{ enroot_top_path_checked }}/enroot_cache"
        backup: true

    - name: Update ENROOT_TEMP_PATH
      become: true
      ansible.builtin.lineinfile:
        dest: /etc/enroot/enroot.conf
        state: present
        regexp: "^#ENROOT_TEMP_PATH.*"
        line: "ENROOT_TEMP_PATH        {{ enroot_top_path_checked }}/enroot_tmp"
        backup: true

    - name: Update ENROOT_SQUASH_OPTIONS
      become: true
      ansible.builtin.lineinfile:
        dest: /etc/enroot/enroot.conf
        state: present
        regexp: "^#ENROOT_SQUASH_OPTIONS.*"
        line: "ENROOT_SQUASH_OPTIONS        -b 262144"
        backup: true

    - name: Update ENROOT_ROOTFS_WRITABLE
      become: true
      ansible.builtin.lineinfile:
        dest: /etc/enroot/enroot.conf
        state: present
        regexp: "^#ENROOT_ROOTFS_WRITABLE.*"
        line: "ENROOT_ROOTFS_WRITABLE        yes"
        backup: true

    - name: Set permissions on {{ enroot_top_path_checked }}
      become: true
      ansible.builtin.file:
        path: "{{ enroot_top_path_checked }}"
        state: directory
        mode: "0777"
        owner: "{{ ansible_user }}"
        group: "{{ privilege_group_name }}"
        recurse: false

    - name: Make sure all {{ enroot_top_path_checked }} directories exist
      become: true
      ansible.builtin.file:
        path: "{{ enroot_top_path_checked }}/{{ item }}"
        state: directory
        mode: "0777"
        owner: "{{ ansible_user }}"
        group: "{{ privilege_group_name }}"
        recurse: false
      with_items:
        - enroot_data
        - enroot_tmp
        - enroot_cache
        - enroot_runtime
