- hosts: compute
  become: true
  vars:
    destroy: true
    initial: false
  vars_files:
    - "/opt/oci-hpc/autoscaling/queues.conf"
  roles:
    - slurm
  when: slurm|default(false)|bool
- hosts: bastion
  become: true
  vars:
    destroy: true
    initial: false
  roles:
    - etc-hosts